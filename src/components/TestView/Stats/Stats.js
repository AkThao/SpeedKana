import ProgressBar from "../ProgressBar/ProgressBar";
import Timer from "../Timer/Timer";

const Stats = () => {
    return (
        <div>
            <ProgressBar />
            <Timer />
        </div>
    );
};

export default Stats;
